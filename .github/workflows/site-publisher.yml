name: Deploy Podval Site Publisher site to GitHub Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v6

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.gradle/ms-playwright
          key: playwright-${{ runner.os }}-ms-playwright

      - name: Generate Site
        run: ./gradlew --no-daemon generateSite

      - name: Upload GitHub Pages from './_site' as an Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy collection-alias Worker
        uses: dubinsky/site-publisher/.github/actions/deploy-alias-worker@master
        with:
          site-path: ${{ github.workspace }}/_site
          hostname: www.alter-rebbe.org
          zone-name: alter-rebbe.org
          account-id: d69a5ee09b435e24288432ec02f45629
          api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          worker-name: alter-rebbe-aliases

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
